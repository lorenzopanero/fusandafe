import 'package:flutter/material.dart';

class FantaPalioScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('FantaPalio'),
      ),
      body: Center(
        child: Text('FantaPalio Screen'),
      ),
    );
  }
}